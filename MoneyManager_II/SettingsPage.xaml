<Page x:Class="MoneyManager_II.SettingsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MoneyManager_II"
      mc:Ignorable="d" 
      d:DesignHeight="1080" d:DesignWidth="1920"
      Title="SettingsPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.2*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Настройки"
           FontSize="36px"
           HorizontalAlignment="Center"
           Foreground="{StaticResource TextColor}"/>

        <StackPanel Grid.Row="1"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Center">
            <Button Name="ColorChangeButton"
                    Content="Включить тёмную тему"
                    FontSize="24px"
                    FontWeight="Bold"
                    Foreground="{StaticResource TextColor}"
                    Background="{StaticResource OneColor}"
                    Width="300px"
                    Height="60px"
                    Margin="0 0 0 50px" Click="Button_Click"/>
            <Button Content="Выйти"
                    FontSize="24px"
                    FontWeight="Bold"
                    Foreground="{StaticResource TextColor}"
                    Background="{StaticResource OneColor}"
                    Width="300px"
                    Height="60px"/>
        </StackPanel>
    </Grid>
</Page>
